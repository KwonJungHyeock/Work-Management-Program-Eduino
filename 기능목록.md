# 에듀이노 사내 통합 업무관리 프로그램 — 구현 기능 목록

> 배포: https://work-manager-liart.vercel.app · 브랜치: `claude/work-automation-draft-sxi5np`
> 이 문서는 기능이 추가/변경될 때마다 함께 업데이트됩니다. (최종: 2026-07-10 · 데이터 코크핏 디자인 · 파트장 3단 가시성 · 계정 비활성화·감사 로그 · 자동 월간 백업 · 전역검색(Ctrl+K)·브라우저 알림 · 동기화 상태·연동 체크리스트)

---

## 🔐 로그인 · 계정 · 권한
| 기능 | 설명 |
|---|---|
| 관리자(팀장) 로그인 | Vercel 환경변수(`EDUINO_ADMIN_CODE`)로 검증, 접속코드는 서버에서만 확인 |
| 팀원 계정 발급/수정/삭제 | 관리자가 아이디·접속코드·이름·부서(CS/MD)·**역할(팀원/파트장)**·업무메일 발급 (`관리자 › 팀원 계정`). **파트장은 직무별 1명 제한**(중복 지정 차단, 서버 이중 방어), 목록에 파트장 배지 |
| **3단 가시성 체계** | 팀장(전체) → **파트장(자기 파트)** → 팀원(본인). 파트장은 사이드바 `파트 관리 › 우리 파트 현황`과 홈 타일로 **자기 부서 업무 현황**만 열람(부서 고정) |
| **계정 비활성화** | 퇴사·휴직 시 삭제 대신 **비활성**(로그인 차단, 상담/발주 기록은 보존). 목록에서 활성/비활성 토글·배지 |
| **감사 로그** | 계정 발급·수정·삭제와 상담/발주 **기록 삭제**를 서버에 기록(누가·언제·무엇을·실행자). `관리자 › 감사 로그`에서 최근 500건 조회 — 내부 통제 |
| 기능별 열람 권한 | 계정마다 사용 가능한 기능을 지정, 권한 없는 메뉴는 잠금(🔒) + 접근 시 "권한 없음" 화면 |
| **관리자 전용 설정 / 팀원 실무 전용** | 모든 설정(연동·상품 마스터·입점사 정보·상담사/분류 편집·결산 양식·설정 백업)은 **관리자만**. 팀원은 실무 화면만 사용 — CS는 상담사가 **본인으로 자동 고정**, 입력 하단 이력은 **본인 것만** 표시 |
| **공유 범위 설정** | 설정마다 공유 대상(전사/CS/MD)을 지정 (`관리자 › 공유 범위`). CS·MD 설정이 서로 섞이지 않고 부서끼리만 동기화됨 |
| **업무 현황 · 인사이트** | 직원별(계정id–이름 연동) 일일 업무량을 리스트+그래프로 조회 (`관리자 › 업무 현황`). **실제 직무 시트(상담/발주 기록)를 기준으로 집계 → 시트에서 삭제하면 즉시 반영**. 종합/CS/MD 탭. 전체 보기 시 CS/MD 일평균·최다담당자 직무별 분리, 직원 리스트 직무별 그룹, 일자별 추이 선 그래프(다크모드 대응). 기간·부서 필터, CSV·차트 PNG 저장. **자동 인사이트 하이라이트**(오늘 급증🔥·이전 동기간 대비 증감·가장 바쁜 날🏆·최다 담당자 비중⭐·후반 추세🚀·무기록일)와 **그래프 hover 툴팁**(날짜·요일별 CS/MD/합계) |
| **인사이트 · 심화 분석** | ① **이상 탐지🔺** — 이번 기간 특정 문의유형(CS)·구매처(MD)가 이전 대비 급증하면 하이라이트 최상단에 경보(전체 비중·증감·원인확인 안내) ② **기간 비교 리포트** — 이번 vs 이전 동기간 CS/MD/합계 증감·% 표 ③ **입점사(구매처) 성과 대시보드** — 발주 건수·비중·이전 대비 증감(▲▼) 랭킹 ④ **CS↔MD 교차** — 같은 기간 CS 문의유형 TOP과 MD 구매처 TOP을 나란히 비교 ⑤ **중국발주 탭** — 중국 발주요청 현황을 관리자 화면에서 분석: 처리 대기·완료·**재고확보 필요(견적문의·입고요청, Loss 위험)**·처리율 KPI + 문의유형/발주요청/상태/상품 분포 + 전체 목록. 기간 지정 입력은 별도 줄로 분리(탭 밀림 방지) |
| 2단 로그인 화면 | 브랜드 히어로 + 입력 폼 (여유로운 대기업형 레이아웃) |

## 🧩 업무편의 부가기능
| 기능 | 설명 |
|---|---|
| **오늘 처리량 위젯** | 상담 메모·발주 입력 화면 상단에 `오늘 나 / 오늘 팀 / 이번 주(팀)` 실적을 실시간 집계해 타일로 표시(서버 기록 기준) |
| **고객 이력 자동 표시** | 상담 메모에서 연락처 입력 시 그 번호의 **과거 상담 이력**(날짜·분류·이름·내용)을 옆에 자동 노출 — 재문의 고객 즉시 파악 |
| **공지 상단 고정(📌필독)** | 관리자가 중요한 공지를 최상단 고정 + `📌필독` 배지 |
| **공지 완료 표시** | 관리자가 업무 완료 처리 시 카드를 연하게(✓완료 배지·취소선·완료일, 완료 취소 토글) |
| **개인 할 일(To-Do)** | 홈 대시보드에 나만 보이는 체크리스트(이 기기 저장) — 추가·완료·삭제 |
| **최근/즐겨찾기** | ⌘K 커맨드 팔레트에 **최근 본 화면**과 **⭐즐겨찾기**(별 토글) 섹션 노출 |
| **테마 자동(시간대)** | 라이트/다크/자동 3단 토글 — 자동 모드는 저녁 7시~아침 7시 다크 전환 |
| **알림 소리** | 새 공지·콜백 도착 시 짧은 비프음(토글, WebAudio) |
| **세션 자동 로그아웃** | 120분 미사용 시 보안상 자동 로그아웃(공용 PC 대비) |

## 🏠 공통 (전 직원)
| 기능 | 설명 |
|---|---|
| 홈 대시보드 | **데이터 코크핏 콘솔** — 다크 상태 헤더(인사·오늘 날짜·라이브 시계·접속 인원 실시간) + 좌측 액센트 바 KPI 타일 + 공지/접속/바로가기 패널 |
| **전역 검색 (Ctrl/⌘+K)** | 어디서든 메뉴 이동 + 이카운트 상품코드·상품명 검색(키보드 탐색·하이라이트). 상단 검색 버튼으로도 실행 |
| **브라우저 알림** | 상단 벨로 켜면, 백그라운드 탭에서도 확인필요 항목(공지·콜백·메모)이 늘면 시스템 알림 |
| **동기화 상태 표시** | 상단바에 구글시트 백업 미전송분 실시간 표시(저장 정상/백업 미전송 N) |
| **알림 센터** | 확인이 필요한 항목을 모아 보여주는 알림 탭. 미확인 공지(각 [확인] 버튼·모두 확인) + 미처리 콜백·나에게 온 메모 바로가기. 사이드바 배지로 개수 표시 |
| 공지사항 | 관리자 작성/게시. **[확인]을 눌러야 읽음 처리**되고 알림·배지에서 사라짐(자동 읽음 아님). **언급(멘션)** — 작성 시 부서(#CS/#MD)·특정 직원(@이름)을 태그하면 카드에 태그가 표시되고, 언급된 직원은 `🔔 나 언급` 강조 + 공개 범위와 무관하게 공지를 받음(알림 배지 반영) |
| 업무 메모/인수인계 | 전체·CS·MD·특정 담당자 지정 메시지 |
| 접속 현황 | 현재 접속 중인 팀원 실시간 표시(3분 TTL) |
| 사이드바 알림 배지 | 미처리 콜백(CS·관리자)·안 읽은 공지(전원) 개수를 메뉴에 빨간 배지로 표시 |
| 월간 데이터 백업 | 관리자 설정 백업 화면에서 선택한 달의 상담·발주 기록 전체를 JSON 파일 하나로 다운로드 |

## 🎧 CS · 고객 상담
| 기능 | 설명 |
|---|---|
| 상담 메모 | 빠른 입력(분류·고객유형·상품분류·**주문경로** 칩). **저장 시 내부 기록+구글시트 동시 전송**. **연락처 한국 전화 서식(서울 02 포함)**, 이름·상품코드 자동완성. 고객유형 학교 세분화(유치원/초/중/고/대) |
| 메일 템플릿 | 고객 메일용 양식을 분류별로 저장·복사 (신규 메뉴) |
| **중국 발주요청** (신규) | 고객이 발주한 중국 제품 중 **재고 없어 대기(주문완료·입고대기)** 또는 **재고 부족으로 상담만 하고 발주 미진행(견적문의·입고요청, Loss 방지)** 건을 기록·공유하는 리스트. 상품코드(품명 자동조회)·문의유형·발주요청·고객명·내용 입력, 문의유형 선택 시 발주요청 자동 매핑. 팀 공유 컬렉션 — **CS 기록, MD·구매가 발주 후 완료 처리**. 문의유형·발주요청·상태 필터, 처리 대기 건수 표시 |
| **상품 조회** | 이카운트 품목을 **상품코드로 정확히 1건 조회**(신규 메뉴). 구매처명·상품분류·입고단가·출고단가 **가로 정렬 표시**(이카운트 화면과 동일 항목)·**제품명 복사**. MD 발주와 동일 카탈로그 공유(매일 00시 최신화). 조회 전용 |
| 상담사 관리 | 상담사 추가/삭제, 고정 칩 선택 |
| 처리 대기(공용 콜백 큐) | 후속조치 필요 건 공유, 담당자 지정·"내 담당" 필터·완료/취소/삭제 |
| **상담 기록(누적 시트)** | 전 상담사의 상담 메모가 서버에 누적. 기간/**상담사·분류·고객유형 필터**/검색 + CSV(보고 싶은 것만 골라 보기). **행 [수정]** 반영, **상담사별 컬러 구분**, 주문경로 컬럼. CS 구성원·관리자 열람 |
| 일일 결산 | **날짜 또는 기간(시작~종료·오늘/최근7일/이번달)으로 집계**. 총건수·콜백건수 + 상담사별·**문의유형별 건수**. 메모장(txt) 저장 + 커스텀 저장 양식 |
| 답변 템플릿 | 응대 문구 관리 · **상담 입력의 답변란에서 [＋ 템플릿 삽입]으로 원클릭 삽입** |

## 📦 MD · 상품 기획
| 기능 | 설명 |
|---|---|
| **이카운트 상품 연동(운영중)** | 상품코드 입력 시 **서버 카탈로그(`/api/catalog`)에서 이카운트 품목을 자동 조회**(품명·입고/출고단가). 매일 00:00(KST) **고정 IP VPS**가 이카운트 OAPI(Zone→로그인→품목)로 전체 품목을 받아 카탈로그 최신화(이카운트 IP 허용목록 대응). 자체 상품 마스터 입력 불필요 — **상품 마스터 화면·코드 자동완성 제거** |
| **이카운트 매핑(구매처·분류 이름표)** | 이카운트가 구매처명·상품분류를 API로 주지 않아, 관리자가 `발주 › 이카운트 매핑`에서 **코드↔이름표**(사업자번호→구매처명, 분류코드→분류명)를 붙여넣어 저장. 팀 전체 공유, 상품 조회·발주에서 코드가 자동으로 이름으로 표시(품명·단가는 API 자동). 관리자 전용 |
| 상세이미지 변환기 | 플랫폼별 이미지 변환 도구 |
| 입점사 발주 | 입점사 배송정책 연동, 배송비 포함 출고송장. **[저장] 버튼 하나로 내부 발주 기록 + 구글시트 동시 반영**. **담당자 선택**(발주 처리자 지정 · 팀원은 본인 자동 고정). **발주 목록 인라인 수정**(일자·구분·담당자·주문자·수량·배송정보를 행에서 바로 고침)(입점사 배송정보 120개). **품명 자동 조회·직접 입력**: 상품코드 입력 시 이카운트 품명이 품명칸에 자동 채워지고(대소문자·공백 무관 매칭), **이카운트 미등록 코드나 품명 없는 품목도 품명을 직접 입력해 그대로 발주에 추가**(목록에 `미등록` 배지 표시) |
| **발주 기록(누적 시트)** | 전 담당자의 발주 내역이 서버에 누적되는 프로그램 내 시트. 기간/담당자/검색 필터 + CSV. MD 구성원·관리자 열람 |

## ⚙️ 백엔드 · 인프라
| 항목 | 구성 |
|---|---|
| Vercel 서버리스 | `/api/store`(설정·접속·공용 컬렉션), `/api/auth`(계정·권한·로스터) |
| Vercel KV (Upstash Redis) | 설정·계정·공지·메모·콜백 서버 저장 (회사 Pro 팀 라이브) |
| 공유 범위별 설정 버킷 | 전사/CS/MD 범위별로 설정 저장·전송, 로그인 사용자는 자기 범위만 수신 |
| 업무 기록 서버 누적 | 상담/발주 저장 시 전체 레코드를 서버 시트(`sheet:<직무>:<시트>:<월>`)에 누적 + 직원×일자 카운터. 편집은 중복 없이 덮어씀, 삭제는 카운터 감소. **메인=프로그램 내 시트, 백업=구글시트** |
| 직무 자동 열람 | 누적 시트는 해당 직무 구성원에게 별도 권한 없이 자동 열람(관리자는 전 직무). 발급된 계정 재편집 불필요 |
| 공용 저장소 자동 연결 | 기본값이 같은 도메인 `/api/store` + 자동 받기 → **직원마다 따로 설정하지 않아도 팀 설정·시트 URL·기록이 자동 공유**(값이 바뀔 때만 반영). 설정은 서버에 저장되어 캐시를 지워도 재접속 시 복원 |
| 저장 실패 자동 재시도 | 순단으로 미전송된 상담/발주를 90초 주기 + 재접속(online) 시 자동 재전송(내부는 멱등 재반영, 외부는 구글시트) |
| 구글시트 백업 ON/OFF | 관리자가 CS/MD 연동 설정에서 구글 백업을 끌 수 있음(끄면 내부 시트에만 저장). 내부 시트 안정화 후 백업 종료 대비 |
| 접속 하트비트 90초 | KV 명령 수 절감(접속 판정 TTL 3분) |
| 일일 리셋 | 입력 화면 '오늘 기록'은 자정이 지나면 자동으로 새 날짜(빈 목록)로 시작 · 누적 기록은 상담/발주 기록 시트에 보존 |
| 연동 설정 | 각 기능 5단계로 통일된 구글 시트 연동 가이드 |
| **자동 월간 백업** | 매월 1일 Vercel 크론(`/api/backup-snapshot`)이 지난달·이번달 상담/발주 시트를 KV 스냅샷(`eduino:backup:<월>`)으로 보관 — 편집·삭제로부터 보호. 설정 백업 화면에 최근 스냅샷 상태 표시 |
| **감사 로그(서버)** | 계정·기록 변경 이력을 `eduino:audit`(최근 500건)에 기록, 관리자 화면에서 조회 |
| **연동 상태 체크리스트** | 설정 백업 화면에서 공용저장소(KV)·CS/MD 시트·이카운트 카탈로그·자동 백업 상태를 한눈에(정상/확인필요 + 상세) |
| 설정 백업/복원 | 연동 상태 확인 + Vercel/구글 동기화 + 자동 업로드 토글 |

## 🎨 디자인 시스템
- 통합 셸(`.mhead/.mtabs/.mbody`) + 다크 사이드바 + 상단바 + 상태바
- 네이비(`#1f56a3`) 기반 신뢰감 색상, 빨강은 로고/오류/MD 부서색으로 제한
- 부서별 식별색(홈/CS/MD/관리자), 대비·가독성 강화
- **디자인 컨셉: 데이터 코크핏**(Retool·Grafana 톤) — 딥 슬레이트 콘솔 크롬 + 라이트 작업영역, 절제된 데이터 팔레트(`--d1~d6`), 재사용 KPI 타일(`.tile` · 좌측 액센트 바 + 큰 수치)
- **홈 대시보드**: 다크 콘솔 상태 헤더(인사 + 오늘 날짜·라이브 시계·접속 인원 실시간) + KPI 타일 행(미확인 공지·새 메모·미처리 콜백·오늘 상담) + 공지/접속/바로가기 패널
- **업무 현황 KPI**: 동일한 좌측 액센트 바 타일로 통일
- **누적 시트 표**: 전체 폭 사용 + 내용·답변 칸이 남는 폭 흡수, 여러 빈 줄은 접어 행 높이 절약
- **전 페이지 폭 통일**: 모든 기능 화면 본문(`.mbody`)이 상단 헤더와 동일하게 전체 폭을 사용하도록 통일 — 페이지마다 달랐던 오른쪽 여백 편차 제거(좌우 24px 정렬)
- **반응형**: 860px 이하에서 사이드바 아이콘 전용으로 자동 축소
- **로그인**: 콘솔 톤 다크 히어로 + 제품 특징 3포인트, 상태바에 앱 버전(`v1.0`) 표기
- **마감 폴리시**: Pretendard 폰트, 레이어드 소프트 섀도우, 진입 애니메이션·포커스 링·선택색, 스켈레톤 로딩, 탭룰러 숫자
- **다크 모드**: 상단 🌙 토글(기기별 저장) — 표면 토큰 재정의로 전 화면 다크 전환(사이드바/콘솔과 통일). 인쇄 시 자동 라이트 출력

---

## 🗺️ 예정 / 확장 가능
- 공유 범위 **개인 단위** 지정 (현재는 부서 단위까지) — 필요 시 부서 위에 얹어 확장
- 기록 데이터(상담 메모·콜백)의 부서 간 열람 제어
